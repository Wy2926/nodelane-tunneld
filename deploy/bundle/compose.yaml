services:
  tunneld:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    volumes:
      - ./releases:/releases:ro
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"
