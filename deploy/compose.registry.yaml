# Registry-only override: use with -f compose.yaml -f compose.registry.yaml.
services:
  tunneld:
    build: !reset null
    image: ${TUNNELD_IMAGE:?Set TUNNELD_IMAGE to an OCI image reference}
    pull_policy: always
